# 이번 세션 트러블슈팅 기록 (2026-09-03)

임시 로그인 관련해서 손댈수록 요구사항이 계속 바뀌었다. 최종 코드만 보면 왜 이 모양인지
알기 어려워서, 그때그때 뭘 고민했고 왜 그렇게 바꿨는지 순서대로 남겨둔다.

---

## 1. "임시 계정 발급" 버튼을 없애고 로그인 폼 하나로 합치기

### 처음 상태
회원가입 백엔드가 아직 없어서, `POST /api/account/issue`라는 별도 엔드포인트로
"임시 계정 발급" 버튼을 누르면 그 자리에서 id/pw를 받아 로그인할 수 있게 만들어 뒀다.

### 고민한 과정
- 처음엔 그냥 빈 `customers` 행에 id/pw만 붙이면 되는 줄 알았다. 근데 그러면 이름도
  이메일도 없는 진짜 빈 계정이 되는 거라 마이페이지 같은 걸 만들어도 보여줄 게 없다.
  이건 아니다 싶어서 — 이미 이름·나이·페르소나가 채워진 **기존 시드 회원**
  (C001~C099) 중 아직 로그인이 없는 사람한테 우선 배정하는 쪽으로 방향을 바꿨다.
- 그런데 이걸 DB를 직접 스크립트로 만져서 "아무 계정이나 골라 id/pw 붙이기" 식으로
  처리하면 안 된다는 걸 깨달았다. 실제 서비스에서 사용자가 누르는 **로그인 흐름
  자체**가 미배정 계정에 자동으로 배정하는 로직을 갖고 있어야 의미가 있다.
- 더 생각해보니 "발급"이라는 별도 단계가 있을 이유가 없었다. 로그인 폼에 처음 보는
  아이디/비밀번호를 치면 그 자리에서 발급 + 로그인이 한 번에 일어나면 되는 거라,
  발급 버튼과 그 엔드포인트를 아예 없애기로 했다.

### 최종 구현
- `app/features/auth.py`의 `issue_account()`를 지우고 그 로직을 `login()` 안으로
  합쳤다. `login(login_id, password)`가 처음 보는 아이디면 발급까지 겸한다.
- `routers/auth.py`의 `POST /api/account/issue` 라우트 삭제.
- 프론트(`ui/menu.js`)의 "임시 계정 발급" 버튼(`#issueBtn`, `#issuedBox`)을 지우고,
  로그인 폼 아래에 안내 문구("처음 쓰는 아이디·비밀번호를 입력하면 그 자리에서
  계정이 만들어집니다.")만 남겼다.

---

## 2. 좋아요·검색 기록이 실제로 DB에 쌓이는지 확인

이슈 기록을 적다 보니 "좋아요/검색 기록이 관리자 대시보드에 쌓인다"고 써놓긴 했는데,
정말 SQLite에 쌓이는지 스스로도 확신이 안 서서 직접 좋아요/검색을 호출해보고
행이 생기는지, 관리자 대시보드(`get_member()`)에서 조회되는지 끝까지 확인했다.
실제로 쌓이고 조회도 되는 걸 확인했고, 테스트로 넣은 행은 지웠다.

---

## 3. 마이페이지 화면 신규 구현

`/api/auth/me` 응답을 기반으로 이메일·가입일 등 회원 정보를 보여주는 마이페이지
화면이 필요해서 아래를 새로 만들었다.

- `Life-Embed-jh/app/core/db.py` — 기존 `customer_one()`을 그대로 재사용.
- `Life-Web/services/engine.py` — `get_customer(customer_id)` 래퍼 추가.
- `Life-Web/routers/auth.py` — `GET /api/auth/me?customerId=...` 추가.
- `Life-Web/frontend/lib/api.js` — `getMe()` 추가.
- `Life-Web/frontend/ui/mypage.js`, `ui/mypage.css` — `ui/history.js`/`history.css`
  구조를 그대로 본떠서 새로 작성. 메뉴에 "마이페이지" 항목 추가.

---

## 4. "마이페이지 눌러도 반응이 없다" → 두 번에 걸쳐 원인을 잘못 짚음

### 1차: "반응이 없다"
직접 재현해보니 실제로는 모달이 뜨고 있었다. 그래서 반응이 없는 게 아니라
**캐시된 이전 `menu.js`/`index.html`이 로드된 탭**이라 그런가보다 하고 강력
새로고침(Ctrl+Shift+R)으로 해결될 거라 생각했다.

### 2차: "마이페이지는 뜨는데 아무런 정보가 안 뜬다"
강력 새로고침 후에도 다시 보니, 모달 자체는 정상 렌더링되는데 로그인된
`customer_id`(`C104`)가 `name`, `email` 등 전부 `null`인 **빈 계정**이라 모든 값이
`-`로만 채워진 거였다. 캐시 문제가 아니라 데이터 문제였던 것 — 1차 진단은 틀렸고,
진짜 원인은 5번 항목의 "빈 계정 생성" 문제로 이어졌다.

### 별도로 고친 것: 로그인 전 마이페이지 접근
"채워진 시드 계정으로 로그인한 마이페이지를 보여주면 되고, 로그인 전에는 마이페이지가
안 뜨게 하면 된다"고 정리해서, `ui/menu.js`의 마이페이지 클릭 핸들러에
`isLoggedIn()` 가드를 추가했다. 비로그인 상태면 마이페이지 대신 로그인 모달이 뜬다.

---

## 5. 빈 계정이 계속 새로 생기는 근본 원인 — "재고 소진"

### 증상
새 아이디로 로그인할 때마다 빈 계정이 계속 만들어지는 게 반복됐다. "왜 또 로그인하면
빈 계정으로 들어가지?" 싶어서 다시 파봤다.

### 원인 분석
1번에서 짠 로직은 "로그인이 없는 기존 회원이 있으면 그 사람에게, 없으면 빈 계정을
새로 만든다"였다. 그런데 이름·나이·페르소나가 채워진 시드 회원은 **99명뿐**이라
재고가 유한했다. 실제 조회해보니:

```
전체 customers: 105
로그인 배정된 계정: 105
로그인 없는(미배정) 계정: 0   ← 재고 완전 소진
빈 계정(이름 없음): 5         ← 소진된 뒤 새로 만들어진 것들
```

로그인 로직 자체가 되돌아간 게 아니라, **1순위(미배정 계정) 재고가 0이 되면서
2순위(빈 계정 생성) 분기가 항상 실행되는 상태**였다는 걸 확인했다.

### 최종 결정
빈 계정을 새로 만드는 건 애초에 있어서는 안 되는 경우라고 판단했다. 반드시 기존
계정 정보에 임시로 id/pw를 붙여서 좋아요·검색·채팅 기록이 쌓이게 해야 하고, 마이페이지는
그 계정의 정보를 보여줄 뿐이니 — 재고가 소진되면 **이미 로그인이 붙어 있는 기존 회원을
여러 아이디가 공유**하더라도, 빈 계정보다는 낫다고 결론냈다.

### 구현
`user_login` 테이블은 원래 `customer_id`가 기본키라 한 회원에 로그인 하나만 붙을 수
있었다. 이걸 `login_id`가 기본키인 구조로 바꿔서, 같은 `customer_id`에 여러 로그인이
붙을 수 있게 했다(기존 데이터는 스키마 마이그레이션으로 보존).

- `app/core/db.py`
  - `ensure_user_login()` — 예전 스키마(`customer_id`가 PK)로 만들어진 DB를 감지하면
    `login_id`가 PK인 새 테이블로 데이터를 옮긴다.
  - `next_unclaimed_customer_id()` / `next_customer_id()` / `create_customer_stub()`
    삭제. 대신 `pick_customer_for_login()` 하나로 통합 — 로그인 없는 회원을 우선하고,
    전부 배정됐으면 **로그인이 가장 적게 붙은 회원**을 다시 골라 쓴다(한쪽에 몰리지
    않도록).
- `app/features/auth.py` — `login()`에서 빈 계정 생성 분기를 완전히 제거하고
  `pick_customer_for_login()` 하나만 호출하도록 단순화.

### 검증
스모크 테스트로 새 아이디 로그인 → 실제 이름 있는 기존 회원(`전기태`, `C001`)에게
배정, 같은 아이디 재로그인 → 같은 계정 유지, 틀린 비밀번호 → 거부됨을 확인했다. 테스트로
만든 로그인 행은 정리했다.

---

## 남은 것

- 이번 세션 이전에 이미 만들어진 빈 계정 5개(`C104` 포함)는 그대로 남아 있다. 삭제
  여부는 아직 안 정했다.
- 이 즉석 발급/재사용 로직 전체는 임시방편이다. 실 회원가입 백엔드가 붙으면
  (`CLAUDE.md` "다음에 할 일 1") 이 분기는 걷어내면 된다.
